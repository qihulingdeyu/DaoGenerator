apply plugin: 'java'
sourceSets {
    main.java.srcDirs = ['src', 'src-template']
    main.resources.srcDirs = ['src', 'src-template']
    test.java.srcDirs = ['tests/java']
    test.resources.srcDirs = ['tests/resources']
}

dependencies {
    compile files('libs/freemarker-2.3.15.jar')
}